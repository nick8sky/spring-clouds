spring:
  cloud:
    zookeeper:
      enabled: true  # true:开启zookeeper外部化配置, false:读取本地配置;
      connect-string: ***:2181
      config:
        #root: /config/service-app
        enabled: true
        watcher:
          enabled: true
  application:
      name: service-app
server:
  port: 9090


#name: hhh

